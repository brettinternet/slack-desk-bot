{
  "name": "slack-desk-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "start": "bun src/index.ts",
    "test": "bun test"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@slack/bolt": "^5.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.19.0",
    "typescript": "latest"
  }
}
